<% content_for :tabs do -%>
 	<li><%=link_to('查看','#tabs-content') %></li>
	<li><%=link_to('收件箱',user_messages_path(@user)) %></li>
	<li><%=link_to('发件箱',sent_box_user_messages_path(@user)) %></li>  
<% end -%>

<p><strong>来自:</strong> <%= @message.sender == @user ? link_to("你", user_path(@user)) : link_to(h(@message.sender.login), user_path(@user)) %></p>
<p><strong>收件时间:</strong> <%= @message.created_at.to_s(:date_time24) %></p>
<p><strong>到:</strong> <%= @message.recipient == @user ? link_to("你", user_path(@user)) : link_to(h(@message.recipient.login), user_path(@user)) %></p>
<p>
  <strong>内容</strong><br />
  <%=h @message.body %>
</p>
<p>
  <% if @message.req == 'recommendation' %>
	<%= link_to '评价对方',new_user_recommendation_path(current_user,:receiver_id => @message.sender )%>   | 
  <% end %>
  <% if @message.recipient == @user %>
	  <%= link_to "回复", new_user_message_path(@user, :reply_to => @message) %> |
  <% end %>
  <%= link_to "收件箱", user_messages_path(@user)%>
</p>