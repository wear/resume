<h1>添加工作信息</h1> 

<% form_for(@position,:url => resume_positions_path(@resume)) do |f| %>
  <%= f.error_messages %>
    <%= render :partial => 'form', :locals => { :f => f }%>
  <p>
    <%= f.submit '创建' %>
  </p>
<% end %>

<%= link_to '返回', edit_resume_path(@resume) %>    

<% content_for :sidebar do %>
   <%= render :comatose=>'cn/position' %>
<% end -%>