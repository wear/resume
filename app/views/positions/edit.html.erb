<h1>编辑工作经验</h1>

<% form_for(@position, :url => resume_position_path(@resume,@position)) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form', :locals => { :f => f }%>
  <p>
    <%= f.submit '更新' %>
  </p>
<% end %> 
<%= link_to '返回', edit_resume_path(@resume) %>    

<% content_for :sidebar do %>

<% end -%>
