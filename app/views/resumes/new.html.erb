

<h2>新建简历</h2>

<% form_for :resume,@resume,:url => resumes_path,:html => { :multipart => true }  do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'form',:locals => { :f => f } %>
  <p>
    <%= f.submit '提交' %>
  </p>
<% end %> 

