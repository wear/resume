<h2>修改用途</h2>

<% form_for @resume,:html => { :multipart => true },:method => :put do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'form',:locals => { :f => f } %>
  <p>
    <%= f.submit '提交' %>
  </p>
<% end %> 

<%=link_to '返回',edit_resume_path(@resume)%>