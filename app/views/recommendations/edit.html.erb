<h3>修改对<%=h @receiver.login %>的评价</h3> 

<% form_for(@recommendation,:url => user_recommendation_path(current_user,@recommendation),:method => :put) do |f| %>   
<%= f.error_messages %>
<%= render :partial => 'form',:locals => {:f => f} %> 
<p>                  
	<%= submit_tag '确定'%>
</p>                                                             	
<% end -%>